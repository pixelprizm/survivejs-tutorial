import Alt from 'alt';

// See the Chrome plugin alt-devtool
//   (https://github.com/goatslacker/alt-devtool) for debugging Alt.  Uncomment
//   the following line, and the `chromeDebug` one below to use it.
// import chromeDebug from 'alt-utils/lib/chromeDebug';

const alt = new Alt();

// chromeDebug(alt);

export default alt;
